86 issues need attention
Security
42
Performance
44

Table `public.survey_responses` is public, but RLS has not been enabled.



Table `public.likes` is public, but RLS has not been enabled.



Table `public.articles` is public, but RLS has not been enabled.



Table `public.categories` is public, but RLS has not been enabled.



Table `public.comments` is public, but RLS has not been enabled.



Table `public.coach_achievements` is public, but RLS has not been enabled.



Table `public.connection_requests` is public, but RLS has not been enabled.



Table `public.coach_specialties` is public, but RLS has not been enabled.



Table `public.analysis_usage` is public, but RLS has not been enabled.



Table `public.focus_areas` is public, but RLS has not been enabled.



Table `public.chat_participants` is public, but RLS has not been enabled.



Table `public.coach_statistics` is public, but RLS has not been enabled.



Table `public.coach_profiles` is public, but RLS has not been enabled.



Table `public.chat_types` is public, but RLS has not been enabled.



Table `public.coach_education` is public, but RLS has not been enabled.



Table `public.direct_chat_connections` is public, but RLS has not been enabled.



Table `public.video_coaches` is public, but RLS has not been enabled.



Table `public.connections` is public, but RLS has not been enabled.



Table `public.chat_rooms` is public, but RLS has not been enabled.



Table `public.performance_metrics` is public, but RLS has not been enabled.



Table `public.sports` is public, but RLS has not been enabled.



Table `public.videos` is public, but RLS has not been enabled.



Table `public.video_feedback` is public, but RLS has not been enabled.



View `public.articles_with_counts` is defined with the SECURITY DEFINER property



Table `public.videos` has RLS policies but RLS is not enabled on the table. Policies include {"Coaches can mark assigned videos as analyzed","Users can delete their own videos","Users can insert their own videos","Users can update their own videos","Users can view their own videos and coaches can view assigned v"}.



Table `public.video_feedback` has RLS policies but RLS is not enabled on the table. Policies include {"Coaches can create feedback for assigned videos","Coaches can update their own feedback","Coaches can view their own feedback","Players can view feedback on their videos"}.



Table `public.video_coaches` has RLS policies but RLS is not enabled on the table. Policies include {"Coaches can update status of their assigned videos","Coaches can view their assigned videos","Players can delete coaches from their videos","Players can insert coaches for their videos","Players can view coaches for their videos"}.



Table `public.survey_responses` has RLS policies but RLS is not enabled on the table. Policies include {"Users can insert their own survey responses","Users can read their own survey responses","Users can update their own survey responses",public_read_safe_fields}.



Table `public.sports` has RLS policies but RLS is not enabled on the table. Policies include {"Authenticated users can insert sports","Authenticated users can update sports","Sports are viewable by everyone"}.



Table `public.performance_metrics` has RLS policies but RLS is not enabled on the table. Policies include {"Users can delete their own performance metrics","Users can insert their own performance metrics","Users can update their own performance metrics","Users can view their own performance metrics"}.



Table `public.focus_areas` has RLS policies but RLS is not enabled on the table. Policies include {"Authenticated users can insert focus areas","Focus areas are viewable by everyone"}.



Table `public.comments` has RLS policies but RLS is not enabled on the table. Policies include {"Enable insert for authenticated users only","Enable read access for all users","delete comments by comment id and author id"}.



Table `public.coach_statistics` has RLS policies but RLS is not enabled on the table. Policies include {"Coach statistics are viewable by everyone","Only system can update coach statistics"}.



Table `public.coach_specialties` has RLS policies but RLS is not enabled on the table. Policies include {"Coach specialties are viewable by everyone","Coaches can delete their own specialties","Coaches can manage their own specialties","Coaches can update their own specialties"}.



Table `public.coach_profiles` has RLS policies but RLS is not enabled on the table. Policies include {"Coach profiles are viewable by everyone","Coaches can insert their own profile","Coaches can update their own profile"}.



Table `public.coach_education` has RLS policies but RLS is not enabled on the table. Policies include {"Coach education is viewable by everyone","Coaches can delete their own education","Coaches can manage their own education","Coaches can update their own education"}.



Table `public.coach_achievements` has RLS policies but RLS is not enabled on the table. Policies include {"Coach achievements are viewable by everyone","Coaches can delete their own achievements","Coaches can manage their own achievements","Coaches can update their own achievements"}.



Table `public.articles` has RLS policies but RLS is not enabled on the table. Policies include {"Allow authenticated users to create articles","Allow public read access for articles"}.



Table `public.analysis_usage` has RLS policies but RLS is not enabled on the table. Policies include {"Admins can view all analysis usage","Users can insert their own analysis usage","Users can view their own analysis usage"}.



Function `public.update_updated_at_column` has a role mutable search_path



Supabase Auth prevents the use of compromised passwords by checking against HaveIBeenPwned.org. Enable this feature to enhance security.



We have detected that the current version of postgres, supabase-postgres-15.8.1.111, has outstanding security patches available. Upgrade your database to receive the latest security patches.


Slow Queries
Query	Avg time	Calls
with records as ( select c.oid::int8 as "id", case c.relkind when $1 then pg_temp.pg_get_tabledef( concat(nc.nspname), concat(c.relname), $2, $3, $4 ) when $5 then concat( $6, concat(nc.nspname, $7, c.relname), $8, pg_get_viewdef(concat(nc.nspname, $9, c.relname), $10) ) when $11 then concat( $12, concat(nc.nspname, $13, c.relname), $14, pg_get_viewdef(concat(nc.nspname, $15, c.relname), $16) ) when $17 then concat($18, nc.nspname, $19, c.relname, $20) when $21 then pg_temp.pg_get_tabledef( concat(nc.nspname), concat(c.relname), $22, $23, $24 ) end as "sql" from pg_namespace nc join pg_class c on nc.oid = c.relnamespace where c.relkind in ($25, $26, $27, $28, $29) and not pg_is_other_temp_schema(nc.oid) and ( pg_has_role(c.relowner, $30) or has_table_privilege( c.oid, $31 ) or has_any_column_privilege(c.oid, $32) ) and nc.nspname IN ($33) order by c.relname asc limit $34 offset $35 ) select jsonb_build_object( $36, coalesce(jsonb_agg( jsonb_build_object( $37, r.id, $38, r.sql ) ), $39::jsonb) ) "data" from records r	1.72s	1
with records as ( select c.oid::int8 as "id", case c.relkind when $1 then pg_temp.pg_get_tabledef( concat(nc.nspname), concat(c.relname), $2, $3, $4 ) when $5 then concat( $6, concat(nc.nspname, $7, c.relname), $8, pg_get_viewdef(concat(nc.nspname, $9, c.relname), $10) ) when $11 then concat( $12, concat(nc.nspname, $13, c.relname), $14, pg_get_viewdef(concat(nc.nspname, $15, c.relname), $16) ) when $17 then concat($18, nc.nspname, $19, c.relname, $20) when $21 then pg_temp.pg_get_tabledef( concat(nc.nspname), concat(c.relname), $22, $23, $24 ) end as "sql" from pg_namespace nc join pg_class c on nc.oid = c.relnamespace where c.relkind in ($25, $26, $27, $28, $29) and not pg_is_other_temp_schema(nc.oid) and ( pg_has_role(c.relowner, $30) or has_table_privilege( c.oid, $31 ) or has_any_column_privilege(c.oid, $32) ) and nc.nspname IN ($33) order by c.relname asc limit $34 offset $35 ) select jsonb_build_object( $36, coalesce(jsonb_agg( jsonb_build_object( $37, r.id, $38, r.sql ) ), $39::jsonb) ) "data" from records r	1.70s	1
with records as ( select c.oid::int8 as "id", case c.relkind when $1 then pg_temp.pg_get_tabledef( concat(nc.nspname), concat(c.relname), $2, $3, $4 ) when $5 then concat( $6, concat(nc.nspname, $7, c.relname), $8, pg_get_viewdef(concat(nc.nspname, $9, c.relname), $10) ) when $11 then concat( $12, concat(nc.nspname, $13, c.relname), $14, pg_get_viewdef(concat(nc.nspname, $15, c.relname), $16) ) when $17 then concat($18, nc.nspname, $19, c.relname, $20) when $21 then pg_temp.pg_get_tabledef( concat(nc.nspname), concat(c.relname), $22, $23, $24 ) end as "sql" from pg_namespace nc join pg_class c on nc.oid = c.relnamespace where c.relkind in ($25, $26, $27, $28, $29) and not pg_is_other_temp_schema(nc.oid) and ( pg_has_role(c.relowner, $30) or has_table_privilege( c.oid, $31 ) or has_any_column_privilege(c.oid, $32) ) and nc.nspname IN ($33) order by c.relname asc limit $34 offset $35 ) select jsonb_build_object( $36, coalesce(jsonb_agg( jsonb_build_object( $37, r.id, $38, r.sql ) ), $39::jsonb) ) "data" from records r	1.69s	1
SELECT name FROM pg_timezone_names	0.10s	77
with f as ( -- CTE with sane arg_modes, arg_names, and arg_types. -- All three are always of the same length. -- All three include all args, including OUT and TABLE args. with functions as ( select *, -- proargmodes is null when all arg modes are IN coalesce( p.proargmodes, array_fill($1::text, array[cardinality(coalesce(p.proallargtypes, p.proargtypes))]) ) as arg_modes, -- proargnames is null when all args are unnamed coalesce( p.proargnames, array_fill($2::text, array[cardinality(coalesce(p.proallargtypes, p.proargtypes))]) ) as arg_names, -- proallargtypes is null when all arg modes are IN coalesce(p.proallargtypes, p.proargtypes) as arg_types, array_cat( array_fill($3, array[pronargs - pronargdefaults]), array_fill($4, array[pronargdefaults])) as arg_has_defaults from pg_proc as p where p.prokind = $5 ) select f.oid as id, n.nspname as schema, f.proname as name, l.lanname as language, case when l.lanname = $6 then $7 else f.prosrc end as definition, case when l.lanname = $8 then f.prosrc else pg_get_functiondef(f.oid) end as complete_statement, coalesce(f_args.args, $9) as args, pg_get_function_arguments(f.oid) as argument_types, pg_get_function_identity_arguments(f.oid) as identity_argument_types, f.prorettype as return_type_id, pg_get_function_result(f.oid) as return_type, nullif(rt.typrelid, $10) as return_type_relation_id, f.proretset as is_set_returning_function, case when f.provolatile = $11 then $12 when f.provolatile = $13 then $14 when f.provolatile = $15 then $16 end as behavior, f.prosecdef as security_definer, f_config.config_params as config_params from functions f left join pg_namespace n on f.pronamespace = n.oid left join pg_language l on f.prolang = l.oid left join pg_type rt on rt.oid = f.prorettype left join ( select oid, jsonb_object_agg(param, value) filter (where param is not null) as config_params from ( select oid, (string_to_array(unnest(proconfig), $17))[$18] as param, (string_to_array(unnest(proconfig), $19))[$20] as value from functions ) as t group by oid ) f_config on f_config.oid = f.oid left join ( select oid, jsonb_agg(jsonb_build_object( $21, t2.mode, $22, name, $23, type_id, -- Cast null into false boolean $24, COALESCE(has_default, $25) )) as args from ( select oid, unnest(arg_modes) as mode, unnest(arg_names) as name, -- Coming from: coalesce(p.proallargtypes, p.proargtypes) postgres won't automatically assume -- integer, we need to cast it to be properly parsed unnest(arg_types)::int8 as type_id, unnest(arg_has_defaults) as has_default from functions ) as t1, lateral ( select case when t1.mode = $26 then $27 when t1.mode = $28 then $29 when t1.mode = $30 then $31 when t1.mode = $32 then $33 else $34 end as mode ) as t2 group by t1.oid ) f_args on f_args.oid = f.oid ) select f.* from f where schema NOT IN ($35,$36,$37) -- source: dashboard -- user: ef17f2f4-0aea-4843-a2a1-3e3c467ac064 -- date: 2025-11-13T18:59:06.053Z	0.32s	17
